# MemType

Address location, which is usually used as the data type of the `mem_type` parameter of MemInfo.

| Value| Description|
| --- | --- |
| MEM_TYPE_DEVICE | The device type is `HOST`.|
| MEM_TYPE_HOST | The device type is `Device`.|
