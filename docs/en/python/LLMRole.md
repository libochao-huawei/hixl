# LLMRole

It represents the role type passed when the `init` API is called.

| Enumerated Value| Meaning|
| --- | --- |
| PROMPT | Prompts (full-graph scenario)|
| DECODER | Decoder (incremental-graph scenario)|
| MIX | Hybrid|
