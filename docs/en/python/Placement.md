# Placement

It indicates the cache location, which is usually used as the data type of the `placement` parameter of CacheDesc.

| Enumerated Value| Meaning|
| --- | --- |
| HOST | The device type is `HOST`.|
| DEVICE | The device type is `Device`.|
