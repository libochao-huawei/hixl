# ----------------------------------------------------------------------------
# Copyright (c) 2026 Huawei Technologies Co., Ltd.
# This program is free software, you can redistribute it and/or modify it under the terms and conditions of
# CANN Open Software License Agreement Version 2.0 (the "License").
# Please refer to the License for details. You may not use this file except in compliance with the License.
# THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
# INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
# See LICENSE in the root of the software repository for the full text of the License.
# ----------------------------------------------------------------------------

set(HIXL_TEST_FILES
        proxy/aclrt_proxy_ut.cc
        proxy/ascend_hal_proxy_ut.cc
        common/thread_pool_ut.cc
        common/json_utils_ut.cc
        common/ctrl_msg_plugin_ut.cc
        proxy/hccp_proxy_ut.cc
        proxy/dcmi_proxy_ut.cc
        common/segment_ut.cc
        llm_datadist_timer_ut.cc
        proxy/dsmi_proxy_ut.cc
        common/log_macro_ut.cc
        cs/endpoint_store_ut.cc
        cs/endpoint_factory_ut.cc
        cs/test_hixl_cs_server.cc
        cs/hixl_cs_client_ut.cc
        cs/hixl_mem_store_basic_ut.cc
        cs/hixl_mem_store_uboe_ut.cc
        cs/hixl_cs_client_basic_ut.cc
        cs/hixl_cs_client_ub_ut.cc
        cs/transfer_pool_ut.cc
        cs/host_register_proxy_ut.cc
        cs/msg_receiver_ut.cc
        engine/hixl_server_unittest.cpp
        engine/hixl_client_unittest.cc
        engine/hixl_utils_unittest.cc
        engine/hixl_options_unittest.cc
        engine/client_handler_config_helper_ut.cc
        engine/client_handler_factory_ut.cc
        engine/hixl_engine_unittest.cc
        engine/hixl_api_unittest.cc
        engine/test_hixl_api.cc
        engine/endpoint_generator_ut.cc
        engine/hixl_profiling_unittest.cc
        engine/hixl_engine_uboe_unittest.cc
        engine/hixl_engine_ubg_unittest.cc
        engine/direct_multi_channel_handler_unittest.cc
        engine/local_comm_res_ut.cc
        engine/route_conf_generator_ut.cc
        engine/tcp_step_barrier_unittest.cc
        ../ops/hixl_kernel/hixl_kernel_basic_unittest.cc
        "${HIXL_CODE_DIR}/benchmarks/comm_benchmark/common/tcp_client_server.cpp"
)

file(GLOB HIXL_SRC_LIST
        "${HIXL_CODE_DIR}/src/hixl/common/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/cs/ubmem/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/engine/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/engine/endpoint_generator/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/cs/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/profiling/*.cc"
        "${HIXL_CODE_DIR}/src/hixl/proxy/*.cc"
        "${HIXL_CODE_DIR}/src/ops/hixl_kernel/*.cc"
)

file(GLOB ADXL_SRC_LIST
        "${HIXL_CODE_DIR}/src/llm_datadist/api/adxl_engine_impl.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/comm_statistic_manager.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/llm_datadist_timer.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/adxl/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/common/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/comm_adapter/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/allocator/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/config/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/span/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/type/*.cc"
        "${HIXL_CODE_DIR}/src/llm_datadist/memory/util/*.cc"
)

set(HIXL_STUB_SRC_FILES
        "${HIXL_CODE_DIR}/tests/depends/llm_datadist/src/ascend_string_stub.cc"
        "${HIXL_CODE_DIR}/tests/depends/llm_datadist/src/data_cache_engine_test_helper.cc"
)

add_executable(hixl_test
        ${HIXL_TEST_FILES}
        ${HIXL_STUB_SRC_FILES}
        ${ADXL_SRC_LIST}
        ${HIXL_SRC_LIST}
        )

add_executable(log_fallback_test common/log_fallback_ut.cc)
target_compile_options(log_fallback_test PRIVATE ${DT_COMMON_COMPILE_OPTION} -Wall -Wfloat-equal)
target_compile_definitions(log_fallback_test PRIVATE _GLIBCXX_USE_CXX11_ABI=0)
target_include_directories(log_fallback_test PRIVATE
        ${HIXL_CODE_DIR}/include
        ${HIXL_CODE_DIR}/src/hixl
        ${HIXL_CODE_DIR}/src/hixl/common
        ${HIXL_CODE_DIR}/src/llm_datadist
)
target_link_libraries(log_fallback_test PRIVATE
        ${ASAN_LINK_OPTION}
        ${GCOV_LINK_OPTION}
        c_sec
        runtime_headers
        metadef_headers
        slog_headers
        GTest::gtest
        GTest::gtest_main
        -pthread
)
set_target_properties(log_fallback_test PROPERTIES CXX_STANDARD 17)

file(GLOB PROFILING_SRC_LIST "${HIXL_CODE_DIR}/src/hixl/profiling/*.cc")
add_executable(profiling_fallback_test
        engine/hixl_profiling_fallback_ut.cc
        ${PROFILING_SRC_LIST})
target_compile_options(profiling_fallback_test PRIVATE ${DT_COMMON_COMPILE_OPTION} -Wall -Wfloat-equal)
target_compile_definitions(profiling_fallback_test PRIVATE _GLIBCXX_USE_CXX11_ABI=0)
target_include_directories(profiling_fallback_test PRIVATE
        ${HIXL_CODE_DIR}/include
        ${HIXL_CODE_DIR}/src/hixl
        ${HIXL_CODE_DIR}/src/hixl/common
        ${HIXL_CODE_DIR}/src/hixl/profiling
        ${HIXL_CODE_DIR}/src/llm_datadist
        ${HIXL_CODE_DIR}/tests
)
target_link_libraries(profiling_fallback_test PRIVATE
        ${ASAN_LINK_OPTION}
        ${GCOV_LINK_OPTION}
        c_sec
        runtime_headers
        acl_rt_headers
        metadef_headers
        slog_headers
        msprof_headers
        error_manager_headers
        msprof_stub
        GTest::gtest
        GTest::gtest_main
        -pthread
)
set_target_properties(profiling_fallback_test PROPERTIES
        CXX_STANDARD 17
        BUILD_RPATH "${CMAKE_BINARY_DIR}/tests/depends/msprof"
)

target_compile_options(hixl_test PRIVATE
        ${DT_COMMON_COMPILE_OPTION}
        -Wno-deprecated-declarations
        -Wall -Wfloat-equal
        )

target_compile_definitions(hixl_test PRIVATE
        HIXL_TEST_SRC_DIR="${CMAKE_CURRENT_SOURCE_DIR}"
        HIXL_ENABLE_TEST_HOOKS
        HIXL_SKIP_DATA_CACHE_ENGINE_TEST_CONTEXT
        HCCN_TEST_CONF_PATH="${HIXL_CODE_DIR}/tests/depends/llm_datadist/src/hccn_test.conf"
        )

target_include_directories(hixl_test PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${HIXL_CODE_DIR}/include
        ${HIXL_CODE_DIR}/src/hixl
        ${HIXL_CODE_DIR}/src/hixl/common
        ${HIXL_CODE_DIR}/src/hixl/proxy
        ${HIXL_CODE_DIR}/src/llm_datadist
        ${HIXL_CODE_DIR}/src/hixl/cs
        ${HIXL_CODE_DIR}/src/hixl/engine
        ${HIXL_CODE_DIR}/src/hixl/engine/endpoint_generator
        ${HIXL_CODE_DIR}/src/hixl/profiling
        ${HIXL_CODE_DIR}/src/ops
        ${HIXL_CODE_DIR}/src/ops/hixl_kernel/extern_include
        ${HIXL_CODE_DIR}/benchmarks
        ${HIXL_CODE_DIR}/benchmarks/comm_benchmark/common
        ${HIXL_CODE_DIR}/tests
        ${HIXL_CODE_DIR}/tests/depends/ascendcl/src
        ${HIXL_CODE_DIR}/tests/depends/hccl/src
        ${HIXL_CODE_DIR}/tests/depends/aicpu/src
        ${HIXL_CODE_DIR}/tests/depends/slog/src
        ${HIXL_CODE_DIR}/tests/depends/msprof/src
        )

set_target_properties(hixl_test PROPERTIES CXX_STANDARD 17)

target_link_libraries(hixl_test
        ${COMMON_LINK_OPTION}
        c_sec
        mmpa_headers
        ascend_hal_headers
        hccl_headers
        runtime_headers
        acl_prof_headers
        msprof_headers
        metadef_headers
        intf_pub
        json
        slog_stub
        mmpa_stub
        msprof_stub
        ascendcl_stub
        runtime_stub
        dcmi_stub
        drvdsmi_host_stub
        hixl_ascend_hal_stub
        sys_api_wrap
        -ldl
        GTest::gtest GTest::gtest_main
        GTest::gmock
        stdc++fs
        -ldl
        -Wl,--no-as-needed
        hccl_stub
        error_manager_stub
        aicpu_stub
        -Wl,--as-needed)

# 让 dlopen("libdcmi.so") / dlopen("libdrvdsmi_host.so") / dlopen("libascend_hal.so") 能找到测试桩
# --disable-new-dtags 强制使用 DT_RPATH（优先于 LD_LIBRARY_PATH），
# 确保测试桩优先于系统 CANN 安装的同名库被加载
set_target_properties(hixl_test PROPERTIES
        BUILD_RPATH "${CMAKE_BINARY_DIR}/tests/depends/dcmi;${CMAKE_BINARY_DIR}/tests/depends/dsmi;${CMAKE_BINARY_DIR}/tests/depends/ascend_hal"
        LINK_FLAGS "-Wl,--disable-new-dtags -Wl,--wrap=dlopen -Wl,--wrap=dlsym -Wl,--wrap=dlclose -Wl,--wrap=dlerror -Wl,--wrap=access -Wl,--wrap=realpath"
)

add_subdirectory(ubmem)

include(CTest)
enable_testing()
